# Core
fastapi==0.135.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1

# LangGraph
langgraph==1.2.9
langgraph-checkpoint-postgres==3.1.0
psycopg[binary,pool]==3.2.3

# LLM
langchain-nvidia-ai-endpoints==1.4.3
# 1.59.0 was never published — PyPI goes 1.58.1 -> 1.59.2. Not a dependency of
# langchain-nvidia-ai-endpoints, which needs no openai at all; kept because
# story 0.2's decision gate allows dropping to the raw client for structured
# output, and the probe scripts already use it.
openai==1.59.2

# Resume parsing
pypdf==5.1.0
python-docx==1.1.2

# Dev
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
