# Core
fastapi==0.135.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
# Required for FastAPI's UploadFile/Form parsing (story 1.2's resume upload).
# FastAPI raises at request time without it, not at import time.
python-multipart==0.0.20
# Story 1.2: the backend talks to Supabase Storage and PostgREST directly
# over raw HTTP rather than pulling in supabase-py, which drags five
# transitive packages in for what is otherwise a handful of REST calls.
# Moved out of # Dev since it is now a runtime dependency, not just a test one.
httpx==0.28.1

# LangGraph
langgraph==1.2.9
langgraph-checkpoint-postgres==3.1.0
psycopg[binary,pool]==3.2.3

# LLM — Groq via its OpenAI-compatible endpoint, since 2026-07-31.
#
# ChatOpenAI(base_url=...) rather than a vendor package, deliberately: that is
# what keeps the NEXT provider swap a one-line change. NVIDIA NIM was dropped
# because with_structured_output returns None there once the system prompt
# passes roughly 1500-2800 characters, on all three of its models, which is the
# shape every agent in this product uses. See DEV-STATE § Decisions 2026-07-31.
langchain-openai==1.1.2
# Pulled up from 1.59.2 by langchain-openai 1.1.2. The old pin comment about
# 1.59.0 never being published is kept in DEV-STATE rather than here; what
# matters now is that the probe scripts under backend/scripts/ still import the
# raw client and have NOT been re-verified against openai 2.x.
openai==2.51.0

# Retained only so the archived NVIDIA probe scripts still import. Nothing in
# backend/app/ imports it as of 2026-07-31. Drop it once those are ported.
langchain-nvidia-ai-endpoints==1.4.3

# Resume parsing
pypdf==5.1.0
python-docx==1.1.2

# Dev
pytest==8.3.4
pytest-asyncio==0.25.0
# Story 3.5.4's token-budget test imports this at MODULE level, so the whole
# file fails to collect without it. It arrives today only as a transitive
# dependency of langchain-openai -- pinned here because a test suite that
# depends on somebody else's dependency tree is one upstream change away from
# a red run nobody caused. Not a runtime dependency: nothing in app/ imports
# it, the probe budget is computed offline in tests only.
#
# `get_encoding("o200k_base")` fetches the BPE file on first use and caches it
# under ~/.cache; a machine that has never run it needs network once.
tiktoken==0.13.0
# httpx moved to # Core above — it is now a runtime dependency too (Supabase
# Storage/REST calls), not just a test one.
