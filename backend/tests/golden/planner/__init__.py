"""Golden cases for the Interview Planner agent.

Run with `make golden AGENT=planner`, or directly with
`pytest tests/golden/planner -v`. See
`docs/specs/agents/AGENT-PLANNER-SPEC.md` §5 for the case table this
package implements -- that spec is the authority; this package is not.

Written BLIND, before `app.agents.planner` exists (story 2.5, ahead of
story 2.6), so the prompt can never be tuned against this suite.
"""
