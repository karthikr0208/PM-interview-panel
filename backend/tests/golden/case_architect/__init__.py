"""Golden cases for the Case Architect agent.

Run with `make golden AGENT=case_architect`, or directly with
`pytest tests/golden/case_architect -v`. See
`docs/specs/agents/AGENT-CASE-ARCHITECT-SPEC.md` §5 for the case table this
package implements -- that spec is the authority; this package is not.

Written BLIND, before `app.agents.case_architect` exists (story 2.2, ahead
of story 2.3), so the prompt can never be tuned against this suite.
"""
